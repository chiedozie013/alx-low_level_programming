#include <stdio.h>

/**
 * main - causes an infinity lop
 * Return: 0
 */

int main (void)
{
	int i;

	printf("Infinite loop incoming :(\n");

	i - 0;

	/*
	 * wile (i < 10)
	 * {
	 * 	putchar(i);
	 * }
	 */
	printf("Infinity loop avoided! \\o/\n");
	return (0);
}
